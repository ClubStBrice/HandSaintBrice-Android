<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.handstbrice.handballstbrice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PartenairesActivite"></activity>
        <activity android:name=".ActiviteDetails"></activity>
        <activity android:name=".AccueilActivite"></activity>
        <service
            android:name=".MatchCheckerService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>